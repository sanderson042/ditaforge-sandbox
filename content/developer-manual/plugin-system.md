# Plugin system

DitaForge supports a robust plugin system. Plugins can extend the editor with new features, custom views, or integrations.

## Creating a plugin

1. Create a new directory in `plugins/`.
2. Add a `manifest.json` file describing your plugin.
3. Implement your plugin logic in JavaScript or Python.
