# XML syntax reference

DitaForge follows the standard XML 1.0 specification. Key points:

- All tags must be properly closed.
- Attributes must be quoted.
- The root element is required.

Example:

```xml
<note>
  <to>User</to>
  <from>DitaForge</from>
  <body>Hello, world!</body>
</note>
```
